<h1>Verbs</h1>
<% @common_verbs.each do |verb| %>
  <div class="word right" id="<%= verb.id %>"><%= verb.name %></div>
<% end %>

<h1>Joiner words</h1>
<% @joiners.each do |joiner| %>
  <div class="word right" id="<%= joiner.id %>"><%= joiner.name %></div>
<% end %>